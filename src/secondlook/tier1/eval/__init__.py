"""Ground-truth eval set and harness for Tier 1 retrieval Modes 1-3.

Produces the eval set and a runnable harness only -- this phase does not
tune any retrieval parameter, threshold, or scoring weight. That is
deliberately left to a later phase, once this harness exists to tune
against.
"""
